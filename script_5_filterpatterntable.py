import os
import pandas as pd
import readline
import functions.microdir as md


"""Script to filter the pattern table (output script 4) with metadata file.
The script take as input the following files:
- pattern table
- metadata file
- metadata filter file (to be compile by the user and available in template directory)

"""
